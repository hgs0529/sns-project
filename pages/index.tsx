import { HeartIcon, SearchIcon } from "@components/common/icon";
import { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div>
      <HeartIcon />
      <SearchIcon />
    </div>
  );
};

export default Home;
